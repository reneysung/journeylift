[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."claude-proxy"]
  timeout = 26

[[headers]]
  for = "/tools/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
